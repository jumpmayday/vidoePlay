# 产品需求文档（PRD）

## Android 本地视频播放器

| 项目     | 内容                        |
| :------- | :-------------------------- |
| 产品名称 | LocalPlay                   |
| 文档版本 | V1.2                        |
| 创建日期 | 2026-07-24                  |
| 更新日期 | 2026-07-24                  |
| 目标平台 | Android 8.0（API 26）及以上 |
| 文档状态 | 与当前实现同步              |
| 包名     | `com.localplay.app`         |

------

## 一、产品概述

### 1.1 产品定位

一款面向 Android 用户的**本地优先视频播放器**：以本地扫描与专业播放为核心，并补充**网页视频嗅探下载**能力，方便把网络资源落到本地后再离线观看。不上传用户视频，不接入广告。

### 1.2 目标用户

- 需要在手机上观看本地下载 / 课程 / 录屏视频的用户
- 有视频片段截取、截图需求的轻度创作者
- 对播放速度、旋转、手势控制有需求的用户
- 希望从网页列表页批量嗅探并下载到本地的用户
- 注重隐私、不希望视频上传云端的用户

### 1.3 核心价值

| 价值点   | 说明                                         |
| :------- | :------------------------------------------- |
| 零门槛   | 授权即扫描，按文件夹聚合本地视频             |
| 强兼容   | 基于 Media3 ExoPlayer，覆盖主流本地容器格式  |
| 专业播放 | 变速、横屏旋转、截图、片段截取、手势调节     |
| 进度记忆 | 随时续播，列表可见进度与续播角标             |
| 嗅探下载 | 粘贴网页地址嗅探直链 / m3u8，后台队列断点续传 |
| 本地优先 | 播放与管理不依赖账号云端；嗅探仅按需联网     |

### 1.4 实现状态说明

本文档中功能按状态标注：

| 标记       | 含义                               |
| :--------- | :--------------------------------- |
| **【已实现】** | 当前 Debug / Release 代码已具备    |
| **【规划中】** | 仍在路线图，未完成或仅部分具备     |

------

## 二、功能需求

### 2.1 权限管理模块 【已实现】

#### 2.1.1 存储权限申请

| 项目     | 说明                                                         |
| :------- | :----------------------------------------------------------- |
| 触发时机 | 首次启动 App 时（`PermissionGate`）                          |
| 权限类型 | Android 13+：`READ_MEDIA_VIDEO`；Android 12 及以下：`READ_EXTERNAL_STORAGE`；Android 9 及以下另需 `WRITE_EXTERNAL_STORAGE` |
| 拒绝处理 | 显示引导页，说明权限用途，提供「去设置」跳转系统权限页       |
| 永久拒绝 | 检测无法再弹系统申请框时，引导用户手动开启                   |

#### 2.1.2 网络与其它权限 【已实现】

| 权限                         | 用途                         |
| :--------------------------- | :--------------------------- |
| `INTERNET` / `ACCESS_NETWORK_STATE` | 网页嗅探与下载               |
| `FOREGROUND_SERVICE` / `FOREGROUND_SERVICE_DATA_SYNC` | 后台下载前台服务             |
| `POST_NOTIFICATIONS`         | 下载进度通知（Android 13+）  |
| `VIBRATE`                    | 截图 / 导出成功轻震动        |

#### 2.1.3 权限状态监听 【已实现】

- 从系统设置返回后重新检测权限状态
- 未授权时停留在权限引导页，不进入主功能

------

### 2.2 视频扫描与列表模块

#### 2.2.1 自动扫描 【已实现】

| 项目         | 说明                                                         |
| :----------- | :----------------------------------------------------------- |
| 扫描触发     | 权限获取后自动执行；首页可手动刷新                           |
| 扫描范围     | `MediaStore.Video` 外部媒体库（排除 `Android/data`、`Android/obb`、隐藏路径等） |
| 扫描方式     | MediaStore 内容查询（当前未做全盘 FileWalker）               |
| 支持格式     | 系统媒体库已索引的视频；播放侧以 ExoPlayer 硬解能力为准      |
| 最小文件过滤 | 默认过滤 &lt; 10MB 或时长 &lt; 5 秒（设置中可调）            |

#### 2.2.2 两级目录导航 【已实现】

采用**两级结构**（非手风琴折叠）：

```
首页：文件夹卡片列表
  ├── 封面（该目录首个视频帧） | 文件夹名 | 视频数量
  └── 点击进入 → 文件夹内视频列表页
        ├── 搜索栏
        └── 视频行：缩略图 | 文件名 | 分辨率·大小 | 更多
```

**首页（文件夹列表）**

| 字段     | 说明                                           |
| :------- | :--------------------------------------------- |
| 封面     | Coil `VideoFrameDecoder` 截取约第 1 秒画面     |
| 名称     | MediaStore `BUCKET_DISPLAY_NAME`               |
| 数量     | 该目录下视频个数                               |
| 交互     | 点击进入文件夹；长按进入多选；支持搜索 / 排序 / 批量删除 |

**文件夹内视频列表**

| 字段         | 说明                                                         |
| :----------- | :----------------------------------------------------------- |
| 视频缩略图   | Coil 视频帧解码（约 1s 处），圆角卡片 + 底部渐变；失败时显示占位 |
| 格式标签     | 扩展名角标（MP4 / MKV 等）                                   |
| 时长角标     | 缩略图右下角 `HH:MM:SS`                                      |
| 续播 / 已看完 | 「续播」「已看完」角标 + 底部细进度条                        |
| 元信息       | 分辨率 · 文件大小                                            |
| 交互         | 点击播放（可续播询问）；长按多选；「…」菜单：播放 / 详情 / 删除 / 批量选择 |

**列表样式（V1.2 补充）**【已实现】

- 视频行使用圆角表面卡片，选中态主题色浅底
- 缩略图约 128×72，叠加渐变与角标，弱化纯灰占位感
- 首页文件夹封面与详情页封面共用同一套视频帧加载组件

**交互补充**

| 能力         | 状态       | 说明                                           |
| :----------- | :--------- | :--------------------------------------------- |
| 搜索         | 【已实现】 | 首页按文件夹名；文件夹内按文件名模糊匹配       |
| 排序         | 【已实现】 | 名称 / 大小 / 时长 / 修改时间等（首页）        |
| 下拉刷新     | 【已实现】 | 首页刷新触发重新扫描                           |
| 批量删除     | 【已实现】 | 首页与文件夹页均支持多选批量删除               |
| 状态栏避让   | 【已实现】 | 主界面使用 `WindowInsets.safeDrawing`          |
| 手动添加目录 | 【规划中】 | 原 PRD「+ 添加目录」尚未实现                   |
| 分享         | 【规划中】 | 长按菜单暂无分享                               |

#### 2.2.3 删除视频文件 【已实现】

| 项目     | 说明                                                         |
| :------- | :----------------------------------------------------------- |
| 入口     | 视频「…」菜单 / 多选批量删除                                 |
| 确认机制 | 二次确认弹窗，展示文件名与大小 / 数量                        |
| 删除方式 | Android 11+ 优先 `MediaStore.createDeleteRequest`；兼容旧路径删除 |
| 删除后   | 列表移除，并清除对应播放进度记录                             |

------

### 2.3 视频播放模块

#### 2.3.1 播放器基础功能 【已实现】（部分项见标注）

| 功能       | 状态       | 说明                                                         |
| :--------- | :--------- | :----------------------------------------------------------- |
| 播放/暂停  | 【已实现】 | 底部控制栏；单击屏幕切换控件显隐                             |
| 进度条拖拽 | 【已实现】 | 底部 Slider                                                  |
| 快进/快退  | 【已实现】 | 双击左/右半屏；步长跟随设置（默认 10s）                      |
| 音量调节   | 【已实现】 | 屏幕**右 1/3** 上下滑动                                      |
| 亮度调节   | 【已实现】 | 屏幕**左 1/3** 上下滑动                                      |
| 中部拖拽   | 【已实现】 | 屏幕中部左右滑动 seek，显示偏移与目标时间提示                |
| 锁屏       | 【已实现】 | 左侧锁图标；锁定后屏蔽手势与大部分控件                       |
| 画面比例   | 【规划中】 | 适应 / 填充 / 16:9 等尚未提供切换入口                        |
| 字幕       | 【规划中】 | 外挂字幕未实现                                               |
| 沉浸横屏   | 【已实现】 | 播放页强制横屏（含重力感应横屏 / 反向横屏）；隐藏系统栏      |

#### 2.3.2 倍数播放 【已实现】

| 项目     | 说明                                                         |
| :------- | :----------------------------------------------------------- |
| 入口     | 控制栏「倍速」                                               |
| 可选档位 | 0.25x / 0.5x / 0.75x / **1.0x** / 1.25x / 1.5x / 2.0x / 3.0x / 4.0x / **5.0x** |
| 交互     | 底部面板选择；长按画面临时 2x（规划中，文案已提示）          |
| 记忆     | 进度记录中可保存倍速；设置中有默认倍速                       |

#### 2.3.3 屏幕旋转 【已实现】（行为已调整）

| 项目         | 说明                                                         |
| :----------- | :----------------------------------------------------------- |
| 默认方向     | 进入播放页即横屏（`SENSOR_LANDSCAPE`），**不做竖屏播放**     |
| 手动旋转     | 控制栏「旋转」：在横屏 ↔ 反向横屏之间切换                    |
| 自动竖屏     | 不支持（产品决策：播放体验统一横屏）                         |

#### 2.3.4 视频截图 【已实现】（V1.2）

| 项目     | 说明                                                         |
| :------- | :----------------------------------------------------------- |
| 入口     | 控制栏「截图」（相机图标）                                   |
| 捕获方式 | 优先 `PixelCopy` / TextureView 当前画面；失败则 `MediaMetadataRetriever` 按当前进度取帧 |
| 保存路径 | `Pictures/LocalPlay/Screenshots/`，文件名：`视频名_时间戳.png` |
| 反馈     | 顶部缩略图预览 + 状态提示 + 轻震动                           |
| 查看     | 点击预览尝试打开系统图库                                     |

#### 2.3.5 视频片段截取（剪辑） 【已实现】（V1.2，简化版）

| 项目       | 说明                                                         |
| :--------- | :----------------------------------------------------------- |
| 入口       | 控制栏「截取」（剪刀图标）                                   |
| 交互流程   | ① 进入截取面板 → ② 拖动进度设当前位置 → ③「设为 A 点」→ ④「设为 B 点」→ ⑤「导出」 |
| 时间精度   | 依赖进度条定位；最短片段 0.5 秒                              |
| 导出策略   | **无损 remux**（`MediaExtractor` + `MediaMuxer`），起止对齐到关键帧，画质与源一致 |
| 导出格式   | MP4 容器写出；音视频轨一并复制（不重编码）                   |
| 保存路径   | `Movies/LocalPlay/Clips/`，文件名含时间戳 `_clip.mp4`        |
| 进度反馈   | 导出按钮显示百分比；完成后 Toast / 状态文案                  |
| 取消       | 导出前可关闭截取面板；导出失败不保留 pending 文件            |
| 未实现项   | 双滑块轴、分辨率重编码、后台通知导出（列为后续增强）         |

#### 2.3.6 其他播放功能

| 功能          | 状态       | 说明                             |
| :------------ | :--------- | :------------------------------- |
| 上一集/下一集 | 【已实现】 | 同文件夹内兄弟文件切换           |
| 播放列表      | 【规划中】 | —                                |
| 后台音频      | 【规划中】 | —                                |
| 硬件解码      | 【部分】   | 设置项已有开关；引擎侧默认硬解   |
| 音轨切换      | 【规划中】 | —                                |

------

### 2.4 播放进度记忆模块 【已实现】

#### 2.4.1 进度记录

| 项目     | 说明                                                         |
| :------- | :----------------------------------------------------------- |
| 记录时机 | 播放中约每 5 秒；退出播放页时保存                            |
| 记录内容 | 路径、位置、时长、大小、修改时间、倍速等                     |
| 存储方式 | Room 数据库                                                  |

#### 2.4.2 进度展示（列表页）

| 展示元素 | 说明                                              |
| :------- | :------------------------------------------------ |
| 进度条   | 缩略图底部细条，主题色 / 已看完为绿色             |
| 续播标记 | 「续播」角标                                      |
| 已看完   | 进度 ≥ 约 95% 显示「已看完」                      |

#### 2.4.3 续播逻辑

- 点击有进度的视频：弹窗「继续播放 / 从头播放」
- 设置中可配置：弹窗询问 / 始终续播 / 始终从头

------

### 2.5 网页嗅探与下载模块 【已实现】（相对初稿新增）

> 扩展定位：本地播放器 + 可选联网拉取资源。嗅探页独立入口，不影响纯本地浏览。

#### 2.5.1 入口与交互

| 项目     | 说明                                                         |
| :------- | :----------------------------------------------------------- |
| 入口     | 首页下载 / 嗅探图标 → `SniffScreen`                          |
| 输入     | 粘贴网页地址（列表页、详情页、播放页或直链均可尝试）         |
| 流程     | 嗅探 → 勾选结果 → 加入后台下载队列 → 可离开页面继续下载      |
| 任务操作 | 暂停 / 继续 / 取消；清理已完成任务                           |

#### 2.5.2 嗅探能力

| 能力               | 说明                                                         |
| :----------------- | :----------------------------------------------------------- |
| 直链检测           | URL 本身为 mp4 / m3u8 等媒体时直接入结果                     |
| 页面解析           | HTML 中提取播放器 JSON（如 `player_aaaa`）、m3u8 / mp4 链接  |
| 站点挑战           | `ChallengedHttpClient`：简易 JS Cookie 门闸、火山引擎类 UA 校验（`ge_ua_p`） |
| 列表页递归         | 从列表页发现详情 / 播放页并继续解析（限制详情页数量）        |
| MacCMS 接口优先    | 对 `listnew/{typeId}.html`、`/list/`、`/type/` 等优先尝试 `/api.php/provide/vod/` 等接口拿 `vod_play_url` |
| 典型站点示例       | 幻雨星类站点详情链（`/v/`、`/p/`）；cdpop.org 类 MacCMS `listnew` 列表 |
| 失败提示           | 403 / IP 限制时明确提示换网络；无结果时返回空列表            |

#### 2.5.3 下载与断点续传

| 项目         | 说明                                                         |
| :----------- | :----------------------------------------------------------- |
| 执行方式     | `DownloadService` 前台服务（`dataSync`）+ Room `download_tasks` 队列 |
| 渐进式文件   | HTTP `Range` + `.part` 临时文件，支持暂停后续传               |
| HLS          | 按分片索引续传拼接                                           |
| 默认保存位置 | MediaStore `Movies/LocalPlay/`（Android 10+）                |
| 自定义路径   | 设置中通过 SAF `OpenDocumentTree` 选择目录并持久化授权       |
| 进程恢复     | App 启动后 `DownloadRepository.startService()` 尝试恢复未完成任务 |

#### 2.5.4 限制与风险（产品需知）

- 目标站点可能封锁机房 / 境外 IP（表现为 403），需用户侧可访问网络
- HTML 结构因站而异，嗅探为启发式，不保证全站成功
- 仅供用户下载其有权访问的内容；合规与版权由用户自行负责

------

### 2.6 设置模块 【已实现】（以当前 UI 为准）

| 设置项           | 说明                         | 默认值              |
| :--------------- | :--------------------------- | :------------------ |
| 下载保存路径     | SAF 选目录 / 恢复默认        | `Movies/LocalPlay`  |
| 默认倍速         | 循环切换常用档位             | 1.0x                |
| 快进/快退秒数    | 5 / 10 / 15 / 30             | 10s                 |
| 最小文件过滤     | MB 阈值                      | 10MB                |
| 最短时长过滤     | 秒阈值                       | 5s                  |
| 续播方式         | 弹窗 / 始终续播 / 始终从头   | 弹窗询问            |
| 硬件解码         | 开关                         | 开                  |
| 自动旋转         | 开关（播放页仍以横屏策略为主） | 开                |
| 清除播放记录     | 清空全部进度                 | —                   |

**【规划中】设置项**：主题切换、截图/片段自定义路径、手动目录管理、画面比例默认值等。

------

### 2.7 视频详情页 【已实现】

长按 / 菜单 →「查看详情」：

| 信息     | 状态                         |
| :------- | :--------------------------- |
| 封面帧   | 【已实现】视频帧缩略图       |
| 文件名   | 【已实现】                   |
| 路径/URI | 【已实现】                   |
| 大小/时长/分辨率/格式/MIME | 【已实现】         |
| 修改时间 | 【已实现】                   |
| 播放进度 | 【已实现】                   |
| 编码/帧率/码率 | 【规划中】需额外探测元数据 |

------

### 2.8 视频格式兼容性

#### 2.8.1 当前策略 【已实现】

- 主引擎：**Media3 ExoPlayer** 硬解本地文件（`MediaItem.fromUri`）
- 扫描源：系统 **MediaStore** 已索引视频
- **未集成** FFmpeg 扩展 / mobile-ffmpeg；冷门格式（如部分 RMVB）可能无法播放

#### 2.8.2 容器与编码（目标能力，部分依赖设备硬解）

| 优先级 | 容器示例        | 说明                 |
| :----- | :-------------- | :------------------- |
| P0     | MP4 / MKV / MOV | 主力格式             |
| P1     | TS / WEBM / AVI | 视设备与 ExoPlayer 支持情况 |
| P2+    | RMVB / VOB 等   | 【规划中】软解扩展   |

------

## 三、非功能需求

### 3.1 性能要求

| 指标         | 要求                                    |
| :----------- | :-------------------------------------- |
| 冷启动时间   | ≤ 2 秒（到列表页可交互）                |
| 视频起播时间 | ≤ 1 秒（本地文件，硬解）                |
| 扫描速度     | 1000 个视频文件 ≤ 10 秒                 |
| 截图响应     | ≤ 500ms（目标）                         |
| 列表滑动     | 60fps；缩略图异步加载，允许首次短暂占位 |
| 嗅探超时     | 单页挑战往返可控；失败有明确文案        |
| 下载续传     | 暂停/杀进程后可从断点恢复               |

### 3.2 兼容性

| 项目         | 要求                                 |
| :----------- | :----------------------------------- |
| 最低系统版本 | Android 8.0（API 26）                |
| 目标系统版本 | Android 15（API 35）                 |
| 屏幕适配     | 手机优先；平板 / 折叠屏【规划中】    |
| 深色 UI      | 当前以深色设计令牌实现（非跟随系统主题引擎） |

### 3.3 安全与隐私

- 不收集账号、不上传用户视频内容
- 不接入广告 / 统计 / 推送 SDK
- 网络权限仅用于用户主动发起的嗅探与下载
- 删除操作需二次确认
- 使用 Scoped Storage / MediaStore / SAF 合规写入截图、剪辑与下载文件

### 3.4 稳定性

- 解码异常时不闪退，给出可理解提示
- 下载失败可重试 / 续传，不损坏已完成文件
- 大文件播放避免无谓全量读入内存

------

## 四、技术方案（与实现对齐）

### 4.1 技术选型

| 层级       | 技术                         | 说明                         |
| :--------- | :--------------------------- | :--------------------------- |
| 语言 / UI  | Kotlin + Jetpack Compose M3  | 全界面 Compose               |
| 播放引擎   | Media3 ExoPlayer 1.5.x       | 本地播放                     |
| 缩略图     | Coil + `coil-video`          | `VideoFrameDecoder` 截帧     |
| 截图       | PixelCopy / Retriever → PNG  | MediaStore Images 写入       |
| 片段截取   | MediaExtractor + MediaMuxer  | 无损 remux                   |
| 数据库     | Room                         | 播放进度、下载任务           |
| 设置       | DataStore Preferences        | 含下载目录 URI               |
| 网络       | OkHttp                       | 嗅探 / 下载 / 站点挑战       |
| 后台下载   | Foreground Service           | `dataSync`                   |
| 权限       | Accompanist Permissions      | 运行时权限门闸               |

### 4.2 模块架构（当前）

```
app/
├── feature/
│   ├── permission/     # 权限引导
│   ├── home/           # 文件夹列表、文件夹内视频列表
│   ├── player/         # 播放、手势、截图/截取面板
│   ├── sniff/          # 嗅探与下载任务 UI
│   ├── settings/       # 设置
│   └── detail/         # 视频详情
├── core/
│   ├── scanner/        # MediaStore 扫描
│   ├── sniff/          # VideoSniffer、ChallengedHttpClient
│   ├── download/       # DownloadService、ResumableDownloadEngine
│   ├── media/          # ScreenshotSaver、VideoClipExporter
│   ├── database/       # Room
│   └── common/         # Formatters 等
├── data/
│   ├── model/
│   └── repository/     # Video / Settings / Download
└── ui/
    ├── components/     # VideoThumbnail 等
    ├── navigation/
    └── theme/          # 深色设计令牌
```

------

## 五、页面结构

```
┌─────────────────────────────────────────────┐
│  权限引导页                                  │
│  └── 首页（文件夹列表）                       │
│       ├── 搜索 / 排序 / 刷新                  │
│       ├── 嗅探入口                            │
│       ├── 设置入口                            │
│       └── 批量删除                            │
│  └── 文件夹视频列表                           │
│       ├── 搜索                                │
│       ├── 视频卡片列表（帧缩略图）            │
│       └── 批量删除 / 详情 / 播放              │
│  └── 播放页（横屏）                           │
│       ├── 手势：亮度 / 音量 / seek            │
│       ├── 控制栏：截取、截图、上下集、倍速、旋转 │
│       ├── 截取面板（A/B 点导出）              │
│       └── 锁屏                                │
│  └── 嗅探下载页                               │
│       ├── URL 输入与嗅探结果                  │
│       └── 后台下载队列（暂停/续传/取消）      │
│  └── 视频详情页                              │
│  └── 设置页（下载路径 + 播放/扫描相关）       │
└─────────────────────────────────────────────┘
```

------

## 六、版本规划（修订）

| 版本     | 范围                                                         | 状态     |
| :------- | :----------------------------------------------------------- | :------- |
| **V1.0** | 权限、MediaStore 扫描、两级目录列表、搜索排序、基础播放与进度记忆、删除、详情、设置骨架 | 已完成   |
| **V1.1** | 倍速、横屏旋转策略、亮度/音量/seek 手势、锁屏、状态栏避让、批量删除 | 已完成   |
| **V1.2** | 网页嗅探下载（含断点续传、可配置路径）、MacCMS/`listnew` 增强、列表视频帧缩略图美化、截图、片段截取 | **当前** |
| **V1.3** | 字幕/音轨、画面比例、手动添加扫描目录、主题、截图预览跳转增强、双滑块精剪 | 规划中   |
| **V2.0** | 平板/折叠屏、播放列表、后台音频、FFmpeg 软解扩展、导出重编码档位 | 规划中   |

------

## 七、验收标准（核心场景）

| #  | 测试场景                         | 预期结果                                       | 状态     |
| :- | :------------------------------- | :--------------------------------------------- | :------- |
| 1  | 首次安装授予视频权限             | 扫描完成，首页按文件夹展示                     | 必测     |
| 2  | 拒绝权限                         | 停留引导页                                     | 必测     |
| 3  | 进入文件夹列表                   | 可见视频帧缩略图（非永久灰块）                 | V1.2 必测 |
| 4  | 播放常见 MP4                     | 横屏起播，进度可拖动                           | 必测     |
| 5  | 左滑亮度 / 右滑音量 / 中滑进度   | 对应提示与效果正确                             | 必测     |
| 6  | 设置 2x 倍速                     | 播放加速                                       | 必测     |
| 7  | 播放中截图                       | PNG 出现在 `Pictures/LocalPlay/Screenshots`    | V1.2 必测 |
| 8  | 截取 ≥ 5 秒片段导出              | `Movies/LocalPlay/Clips` 可播放                | V1.2 必测 |
| 9  | 播放至中途退出                   | 列表显示续播角标与进度条                       | 必测     |
| 10 | 再次点击该视频                   | 可续播或从头                                   | 必测     |
| 11 | 删除视频确认                     | 文件与列表项移除，进度清除                     | 必测     |
| 12 | 嗅探可访问站点列表页并下载       | 结果可选中，后台队列进度更新，支持暂停续传     | V1.2 必测 |
| 13 | 设置自定义下载目录后下载         | 文件写入所选 SAF 目录                          | V1.2 必测 |
| 14 | 嗅探遇 403                       | 明确提示 IP/地区限制，不崩溃                   | V1.2 必测 |

------

## 八、风险与对策

| 风险                      | 影响           | 对策                                           |
| :------------------------ | :------------- | :--------------------------------------------- |
| 设备硬解兼容差            | 花屏 / 失败    | 后续设备黑名单 + 软解扩展                      |
| 站点反爬 / IP 墙          | 嗅探失败       | 挑战客户端 + 明确 403 文案；用户换网络         |
| 站点 HTML 变更            | 解析失效       | MacCMS API 优先；持续补充详情/播放页正则       |
| remux 对齐关键帧         | 截取起点不准   | 产品说明「按关键帧对齐」；后续可加重编码精剪   |
| 缩略图首解耗时            | 列表首次略卡   | Coil 缓存；取 1s 帧避开部分黑场                |
| 厂商限制后台              | 下载中断       | 前台 Service + 通知；进程拉起恢复队列          |
| 存储权限分版本差异        | 删改失败       | MediaStore / SAF 双路径                        |

------

## 九、附录

### 9.1 竞品参考

| 产品            | 借鉴点                       |
| :-------------- | :--------------------------- |
| MX Player       | 手势、解码策略               |
| VLC for Android | 格式兼容、字幕               |
| nPlayer         | 倍速与本地体验               |
| 系统自带播放器  | MediaStore 集成、简洁列表    |

### 9.2 名词解释

| 术语           | 说明                                                    |
| :------------- | :------------------------------------------------------ |
| 硬解           | 使用设备硬件解码器，性能高功耗低                        |
| remux          | 不重编码，仅重新封装音视频轨到新容器                    |
| ExoPlayer      | Google Media3 播放组件                                  |
| MediaStore     | 系统媒体索引                                            |
| SAF            | Storage Access Framework，用户授权目录访问              |
| MacCMS         | 常见影视站程序，常提供 `/api.php/provide/vod/` JSON API |
| 断点续传       | 使用 HTTP Range / 分片索引从中断处继续下载              |

### 9.3 设计令牌（当前深色主题）

| Token     | 色值（约） | 用途     |
| :-------- | :--------- | :------- |
| LpBg      | `#0E1116`  | 页面底   |
| LpPrimary | `#2DD4BF`  | 主强调色 |
| LpSurface | 深灰层次   | 卡片表面 |
| LpThumb   | `#374151`  | 缩略图占位 |

### 9.4 修订记录

| 版本 | 日期       | 说明                                                         |
| :--- | :--------- | :----------------------------------------------------------- |
| V1.0 | 2026-07-24 | 初稿                                                         |
| V1.2 | 2026-07-24 | 对齐实现：两级导航、手势与横屏策略、嗅探下载与断点续传、列表视频帧缩略图、截图与片段截取；区分已实现/规划中 |

------
